# Variables de entorno para PostgreSQL local
# Copia este archivo a .env.local y ajusta los valores

# ========================================
# CLERK AUTHENTICATION
# ========================================
# Obtén estas claves en: https://dashboard.clerk.com/
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# URLs de redirección de Clerk (opcionales - se detectan automáticamente)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/iniciar-sesion
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/registro
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/admin
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/admin

# ========================================
# DATABASE - POSTGRESQL (Neon)
# ========================================
# URL de conexión principal (usa esta para Prisma y conexiones directas)
DATABASE_URL=postgresql://usuario:password@host.neon.tech/dbname?sslmode=require

# Para PostgreSQL local (desarrollo)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=researcher_platform
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# Para Vercel Postgres (se configuran automáticamente en Vercel)
POSTGRES_HOST=tu-host-vercel
POSTGRES_DATABASE=tu-database-name
POSTGRES_USER=tu-username
POSTGRES_PASSWORD=tu-password
POSTGRES_PORT=5432

# ========================================
# SECURITY
# ========================================
JWT_SECRET=1b7e2c8e-4f3a-4e9a-9c2d-8e7f2a6b1c3d
NODE_ENV=development

# Vercel Blob: token con permisos para usar la API de Blob. Añade esto si usas Vercel Blob
# Crea un token en https://vercel.com/account/tokens y ponlo aquí como VERCEL_TOKEN
# Ejemplo: VERCEL_TOKEN=vercel_abcdef123456...
VERCEL_TOKEN=

# ========================================
# EMAIL NOTIFICATIONS (SMTP) - OPCIONAL
# ========================================
# Configurar para enviar notificaciones por correo
# Si no se configura, los mensajes solo funcionarán dentro de la app

# Gmail (Opción 1 - Requiere "App Password")
# 1. Activa verificación en 2 pasos en tu cuenta Google
# 2. Genera una "App Password" en https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password-aqui
SMTP_FROM="SECCTI <tu-email@gmail.com>"

# Outlook/Hotmail (Opción 2)
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=tu-email@outlook.com
# SMTP_PASS=tu-password
# SMTP_FROM="SECCTI <tu-email@outlook.com>"

# SendGrid (Opción 3 - Recomendado para producción)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=tu-sendgrid-api-key
# SMTP_FROM="SECCTI <noreply@tudominio.com>"

# Mailgun (Opción 4)
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@tudominio.mailgun.org
# SMTP_PASS=tu-mailgun-password
# SMTP_FROM="SECCTI <noreply@tudominio.com>"

# ========================================
# OCR & PDF PROCESSING (Opcional)
# ========================================
# Para desarrollo local:
PDF_PROCESSOR_URL=http://localhost:8001
TESSERACT_CMD=/opt/homebrew/bin/tesseract

# Para producción:
# PDF_PROCESSOR_URL=https://tu-servidor-ocr.railway.app
